


// This is a template for how to make a bot using this repository.
// the bot function will be called everytime the board updates.
// you can call the move function to make a move.

bot = function(board) {
	// move([board[1][0], board[0][0]]);
}